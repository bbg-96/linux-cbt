// 게스트 대화형 셸의 공통 초기화 조각.
//
// 컬러 PS1 — busybox ash가 \[ \] \e 프롬프트 이스케이프를 지원함을 실VM으로 검증했다
// (\[ \]는 프롬프트 폭 계산에서 제외하는 마커 — 없으면 긴 명령 편집 시 커서가 어긋난다).
// 항상 root 단일 사용자 시스템이라 말미는 리터럴 "# "를 쓴다. waitPrompt()는 CSI
// 시퀀스를 걷어낸 뒤 매칭하므로 색 코드가 프롬프트 감지를 깨지 않는다.
export const PS1_INIT =
  "export PS1='\\[\\e[1;32m\\]root@\\h\\[\\e[0m\\]:\\[\\e[1;34m\\]\\w\\[\\e[0m\\]# '";
