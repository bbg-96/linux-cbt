# 스테이징 빌드 전용 (vite --mode staging 이 자동으로 읽는다)
# src/problems/index.ts 가 이 값을 보고 STAGING_PROBLEMS 를 사용한다.
VITE_PROBLEM_SET=staging
# 게스트 이미지 프로필: systemd 계열 명령(hostnamectl·timedatectl·lsblk)을 쓰려면 debian.
# 운영은 이 값이 없어 alpine 을 그대로 쓴다 (검증된 30문제 환경 보존).
VITE_IMAGE_PROFILE=debian
